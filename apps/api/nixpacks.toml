[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["corepack enable && corepack prepare pnpm@9.0.0 --activate", "pnpm install --filter @repo/api..."]

[phases.build]
cmds = ["pnpm run build --filter @repo/api"]

[phases.start]
cmd = "node apps/api/dist/main"
